import { prisma } from "@/lib/prisma";
export default async function Home(){const products=await prisma.product.findMany({where:{active:true},orderBy:{createdAt:"desc"}});return <main><header className="header"><nav className="nav"><strong>🐟 ขายปลาออนไลน์</strong><span>ตะกร้า · คำสั่งซื้อ</span></nav></header><div className="container"><section className="hero"><h1>ปลาสด ส่งตรงถึงบ้าน</h1><p className="muted">เลือกปลาที่ต้องการ แล้วสั่งซื้อออนไลน์ได้ทันที</p></section><section className="grid">{products.map(p=><article className="card" key={p.id}><div className="image">🐟</div><h2>{p.name}</h2><p className="muted">{p.description}</p><p>คงเหลือ {p.stock} ตัว</p><div className="price">฿{Number(p.price).toLocaleString("th-TH")}</div><a className="button" href={`/products/${p.slug}`}>ดูรายละเอียด</a></article>)}</section></div></main>}
